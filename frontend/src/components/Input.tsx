const Input = () => {
    return (
        <textarea
            placeholder="Ask anything"
            className="input-field"
            wrap="soft"
            cols={12}
        />
    );
}

export default Input;